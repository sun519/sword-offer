function TreeDepth(pRoot) {
    if(pRoot === null) {
        return 0;
    }
    if(pRoot) {
        var left = pRoot.left;
        right = pRoot.right;
        return Math.max(TreeDepth(left),TreeDepth(right)) + 1;
    }   
}